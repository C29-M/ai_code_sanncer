# prompt_guard environment configuration
# Copy to .env and customize as needed

# Logging level: DEBUG, INFO, WARNING, ERROR
PROMPT_GUARD_LOG_LEVEL=INFO

# Risk threshold (prompts scoring above this are marked unsafe)
PROMPT_GUARD_RISK_THRESHOLD=5.0

# Scan timeout in seconds
PROMPT_GUARD_TIMEOUT=30
