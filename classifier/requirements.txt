fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# HuggingFace — CPU-only torch installed separately in Dockerfile
transformers==4.40.2
tokenizers==0.19.1

# Redis cache (optional — service degrades gracefully if Redis is absent)
redis==5.0.4

# Utilities
numpy==1.26.4
