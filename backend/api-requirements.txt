# API framework dependencies only.
# Scanner tools (semgrep, bandit, safety, trufflehog) are installed
# separately in the Dockerfile with pinned versions to avoid conflicts.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gitpython>=3.1.40
pydantic>=2.9.0
httpx>=0.27.0
